"use strict";

/* 2.	 

use map and an arrow function to get a new array with the squares of each element -- i.e., [1, 4, 9]
*/
const array123 = [1, 2, 3]
console.log("map and arrow squares :" ,array123.map(num => num * num));
